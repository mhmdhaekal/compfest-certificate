<!DOCTYPE html>

<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  @vite(['resources/js/app.js'])
  <title>{{title || "COMPFEST Certificate"}}</title>
  
  @if ($slots.meta)
  {{{ await $slots.meta() }}}
  @endif
</head>

<body class="bg-background antialiased" hx-boost="true">
  <div class="flex min-h-screen flex-col overflow-hidden">
    <div class="relative z-10 flex flex-grow justify-center">
      {{{ await $slots.main() }}}
    </div>
  </div>
</body>

</html>
